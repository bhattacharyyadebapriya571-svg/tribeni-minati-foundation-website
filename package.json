{
  "name": "tribeni-minati-foundation-website",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm --prefix my-app run dev",
    "build": "npm --prefix my-app run build",
    "preview": "npm --prefix my-app run preview",
    "nemotron": "node scripts/nemotron.mjs"
  },
  "dependencies": {
    "react-animista": "^1.0.2"
  },
  "devDependencies": {
    "@types/react": "^19.2.18",
    "@types/react-dom": "^19.2.5"
  }
}
